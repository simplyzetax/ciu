[package]
name = "ciu-helmet"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1"
log = "0.4"

esp-idf-hal = "0.46.2"
esp-idf-svc = { version = "0.52.1", features = ["binstart"] }

ciu-core = { path = "../core" }

[build-dependencies]
embuild = "0.33.5"
