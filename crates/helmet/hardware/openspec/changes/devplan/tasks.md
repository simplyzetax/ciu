# Tasks

- [ ] Inspect the existing root `DEVPLAN.md` and all DEVPLAN/bring-up references to avoid conflicting sources of truth.
- [ ] Validate this proposal.
- [ ] Write the minimum complete `docs/DEVPLAN.md`, explicitly preserving the 3.3 V-only, current-budget, two-layer, RF-validation, and unverified-part constraints.
- [ ] Record the planning rationale in `docs/DECISIONS.md` and the run in `docs/CHANGELOG.md`.
- [ ] Run documentation drift checks; run ERC/DRC only if finish requires confirmation because no KiCad source is changing.
- [ ] Finish only after all required gates are clear.
