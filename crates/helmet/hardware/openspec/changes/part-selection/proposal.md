# Proposal: part-selection

> Marker: AUTO (autonomous mode; auto-approved, reviewable after the fact)

## Why

Select sourceable, symbol-resolvable components for the minimal AT86RF233-ZU carrier before schematic capture.

## What Changes

- Add docs/BOM.md with one row per refdes and the required fixed columns.
- Record part-selection rationales and current-budget checks in design documentation.
- Confirm installed KiCad symbols and real pins for U1, connector, antenna, and other active parts before committing them.
