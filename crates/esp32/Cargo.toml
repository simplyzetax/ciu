[package]
name = "ciu-esp32"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1"
esp-idf-svc = { version = "0.52.1", features = ["binstart"] }
ciu-core = { path = "../core" }

[build-dependencies]
embuild = { version = "0.33.5", features = ["espidf"] }
